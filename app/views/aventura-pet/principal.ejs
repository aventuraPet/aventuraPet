<main>
        
     <div class="profile-card">
        <div class="profile-image">
            <img src="data:image/png;base64, <%= data[0].img %>"
                alt="Foto de {{nome}}" />
        </div>
        <% //console.log(data) %>
        <div class="profile-info">
            <div class="top-info">
                <h2><%= data[0].nome_pet, data[0].idade %></h2>
                <p class="location">📍 <%= data[0].cidade %></p>
            </div>

            <input type="hidden" value="<%= data[0].idUserPet %>" id="idUserPet">

            <div class="caracteristicas">
                <p><strong>Caracteristicas:</strong> <%= data[0].caracteristica %></p>
                <p><strong>Distância:</strong> <%= data[0].distancia %></p>
            </div>
        </div>

        <div class="botoes">
            
            <a href="/aventura-pet/view-pets/dislike?idUserPet=<%= data[0].idUserPet %>" id="dislike" class="btn dislike">✖️</a>
            
            <a href="/aventura-pet/view-pets/like?idUserPet=<%= data[0].idUserPet %>" id="like" class="btn like">💚</a>
            <a href="https://wa.me/55<%= data.telefone.replace(/[()\s-]/g,'')%>?text=ola!gostaria de adotar seu pet <%= data.nome_pet %> que localizei na plataforma aventura-pet" id="send" class="btn send" target="_blank">📨</a>
        </div>
    </div>
</main>
<script>
    document.addEventListener("DOMContentLoaded", function(){
        var send = document.getElementById('send');
        send.addEventListener("click", async function(){
            var idUserPet = document.getElementById("idUserPet");
            console.log(idUserPet);
            await fetch(`/aventura-pet/view-pets/like?idUserPet=${idUserPet.value}`,{
                method:'get'
            })
            .then(response =>{
                if(response.ok){
                    location.reload(true);   
                }
            })
        })
    })
</script>